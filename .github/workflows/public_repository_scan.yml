name: check file content

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest


    steps:
      - name: Git Clone Action
        # You may pin to the exact commit or the version.
        # uses: sudosubin/git-clone-action@8a93ce24d47782e30077508cccacf8a05a891bae
        uses: sudosubin/git-clone-action@v1.0.1
        with:
          # Repository owner and name. Ex: sudosubin/git-clone-action
          repository: ayumioka/wiki # optional, default is ${{ github.repository }}
          # Git host platform. Ex: github, gitlab, bitbucket, gitee, or else (git.suckless.org, ...)
          #platform: # optional, default is github
          # Git branch or tag to checkout.
          #ref: # optional
          # Relative path from current directory, where to clone.
          #path: # optional
        
      - name: PowerShell Core Action
        uses: actions/checkout@v3
        run: Get-ChildItem -R | Select-String -Pattern "password", "pwd", "apikey" -List | Select-Object Path
        shell: pwsh

      # - name: Azure PowerShell Action
      #   uses: Azure/powershell@v1
      #   with:
      #     # Specify the Az PowerShell script here.
      #     inlineScript: Get-ChildItem -R | Select-String -Pattern "password", "pwd", "apikey" -List | Select-Object Path
      #     # Azure PS version to be used to execute the script, example: 1.8.0, 2.8.0, 3.4.0. To use the latest version, specify "latest".
      #     azPSVersion: latest
      #     # Select the value of the ErrorActionPreference variable for executing the script. Options: stop, continue, silentlyContinue. Default is Stop.
      #     #errorActionPreference: # optional, default is Stop
      #     # If this is true, this task will fail if any errors are written to the error pipeline, or if any data is written to the Standard Error stream.
      #     #failOnStandardError: # optional, default is false
      #     # Used to pull Az module from Azure/az-ps-module-versions.  Since there's a default, this is typically not supplied by the user.
      #     #githubToken: # optional, default is ${{ github.token }}
        
